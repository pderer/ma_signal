name: ETF Signal Bot

on:
  schedule:
    - cron: '0 7 * * 1-5'  # 한국 16:00
  workflow_dispatch:       # 수동 실행 버튼

permissions:
  contents: write  # state.json 업데이트 위해

jobs:
  run:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Python 설치
        uses: actions/setup-python@v4
        with:
          python-version: '3.13.13'

      - name: 라이브러리 설치
        run: |
          pip install -r requirements.txt

      - name: 실행
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          KONGJI_TELEGRAM_TOKEN: ${{ secrets.KONGJI_TELEGRAM_TOKEN }}
          KONGJI_TELEGRAM_CHAT_ID: ${{ secrets.KONGJI_TELEGRAM_CHAT_ID }}
        run: |
          python main.py

          echo "===== state.json ====="
          cat state.json

          echo "===== git diff ====="
          git diff -- state.json

      - name: Debug versions
        run: |
          python --version
          pip show yfinance
          pip show curl_cffi
          pip show pandas
          pip show numpy

      - name: Commit state
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add state.json
          git commit -m "Update state" || echo "No changes to commit"
          git push
